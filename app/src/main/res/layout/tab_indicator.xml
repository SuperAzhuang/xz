<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content" >

    <TextView
        android:id="@+id/tab_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:drawableTop="@mipmap/item1"
        android:gravity="center"
        android:textColor="@color/primarybar_txt"
        android:textSize="12sp" />

    <!--
         <ImageView
        android:id="@+id/tab_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:padding="8dp" />
    -->

    <TextView
        android:id="@+id/tab_mes"
        android:layout_width="6dp"
        android:layout_height="6dp"
        android:layout_alignRight="@id/tab_title"
        android:layout_alignTop="@id/tab_title"
        android:layout_marginTop="2dp"
        android:text="1111"
        android:drawableLeft="@drawable/point1"
      />

</RelativeLayout>